package com.intracom.ems.workshop;

import javax.ws.rs.Path;

@Path("/link")
public class LinkResource {
    /*
     * 11th Exercise: Get Links
     * Write a jax-rs GET method that will retrieve the links from the
     * database. Add missing params and annotations.
     */
    @Path("/get")
    public String getLinks() {
        return null;
    }
}
